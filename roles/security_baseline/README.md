# security_baseline

This role provides a generic security baseline (SSH hardening, firewall, fail2ban).

This public repo version is being ported and sanitized from a private repo.

## Privacy/safety
- No real inventory, IPs, hostnames, fingerprints, or provider metadata are included.
- All site-specific access rules should be defined in your private `inventory/` (gitignored).
