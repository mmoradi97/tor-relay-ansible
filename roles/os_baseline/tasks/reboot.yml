---
- name: Check if reboot is required
  ansible.builtin.stat:
    path: /var/run/reboot-required
  register: os_baseline_reboot_required

- name: Reboot if required
  ansible.builtin.reboot:
    msg: "Reboot initiated by os_baseline"
    reboot_timeout: 900
  when:
    - os_baseline_reboot_if_required | bool
    - os_baseline_reboot_required.stat.exists
