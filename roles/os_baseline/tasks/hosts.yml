---
- name: Ensure 127.0.0.1 maps to localhost + hostname
  ansible.builtin.lineinfile:
    path: /etc/hosts
    regexp: '^127\\.0\\.0\\.1\\s+'
    line: "127.0.0.1 localhost {{ os_baseline_hostname }}"
    state: present
    backup: true
  when: os_baseline_manage_hosts_file | bool
