# app_snowflake

Runs a Snowflake proxy using Docker Compose.

## Privacy/safety
- This public repo contains no real host inventory, IPs, or operator identifiers.
- Any operational secrets or environment-specific settings must be provided via your private `inventory/` (gitignored) or environment variables.
